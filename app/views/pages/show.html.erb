<p id="notice"><%= notice %></p>

<h2><%= @page.title %></h2>

<p class="img-indent-bot"><%= raw(@page.content) %></p>

<%if current_user %>
  <%= link_to 'Edit', edit_page_path(@page) %> |
  <%= link_to 'Back', pages_path %>
<% end %>